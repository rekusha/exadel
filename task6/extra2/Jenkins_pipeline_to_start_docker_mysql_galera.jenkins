pipeline {
    agent {
        label 'ubuntu' 
    }

    environment {
        WORK_DIR = './mysql-cluster'
    }
    stages {
      stage(‘Build’) {
        steps {
	  dir(WORK_DIR) {
          sh 'ansible-playbook playbook.yml' 
          }
        }
      }
    }
}
